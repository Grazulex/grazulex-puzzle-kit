{
  "name": "grazulex-puzzle-kit-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "turbo": "^2.3.0"
  }
}
